struct Person {
    age: Int
}
fn a(p: Person, b: Person) -> Person {
    return if p.age > b.age {
        p
    } else {
        b
    };
}


let per1 = Person {age: 3};
let per2 = Person {age: 4};
let age = a(per1, per2);