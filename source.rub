struct Person {
    age: Int,
}

let p = Person {age: 3};
let p2 = Person {age: 3};

fn test(per: Person, per2: Person) -> Bool {
    per.age == per2.age
}

if test(p, p2) {
    print(1);
} else {
    print(0);
}

